{
  "name": "currency-dashboard",
  "version": "1.0.0",
  "description": "Real-time currency dashboard with smart revalidation",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:backend": "cd apps/backend && npm run start:dev",
    "build": "npm run build:shared && npm run build:backend && npm run build:frontend",
    "build:frontend": "cd apps/frontend && npm run build",
    "build:backend": "cd apps/backend && npm run build",
    "build:shared": "cd packages/shared && npm run build",
    "install:all": "npm install && cd apps/frontend && npm install && cd ../backend && npm install && cd ../../packages/shared && npm install",
    "clean": "rm -rf apps/*/node_modules apps/*/dist packages/*/node_modules packages/*/dist node_modules",
    "test": "cd apps/backend && npm test",
    "lint": "cd apps/backend && npm run lint",
    "start:prod": "cd apps/backend && npm run start:prod"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}